import React from 'react'

import { useAuth } from '../../../context/AuthContext';


const Main = () => {

  const { signup, currentUser } = useAuth();
  return (
    <div>
      
    </div>
  )
}

export default Main
