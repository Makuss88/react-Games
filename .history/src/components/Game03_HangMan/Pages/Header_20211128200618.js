import React from 'react'
import { STAR_COMPONENT } from '../../as';

const Header = () => {


const dupa = const state = useSelector(state => state.state)

  return (
    <>
      <STAR_COMPONENT />
      <h1> HANGMAN </h1>
    </>
  )
}

export default Header
